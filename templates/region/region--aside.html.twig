{#
/**
 * @file
 * Overridden implementation to display the aside region.
 */
#}
{% if content %}
  {{ attach_library('ooe/region--' ~ region) }}
  <aside{{ attributes }}>
    <div class="region__content">
      {{ content }}
    </div>
  </aside>
{% endif %}
